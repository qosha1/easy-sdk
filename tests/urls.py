"""
URL configuration for testing.

This is a minimal URL configuration for pytest-django.
"""

from django.urls import path

# Minimal URL patterns for testing
urlpatterns = [
    # Empty URL patterns - we don't need any actual URLs for library testing
]